Drop your poster images in this folder (e.g. `1.jpg`, `2.jpg`, ...), then
reference them in `src/data/images.js` as `/images/<filename>`.

Recommended: portrait images, ~1200x1800px, JPG or WebP, optimized for web.
